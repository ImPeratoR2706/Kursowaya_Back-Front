# Базовый URL Django API (без завершающего слэша).
# Для npm run dev можно оставить пустым: Vite проксирует /api на http://127.0.0.1:8000 (см. vite.config.ts).
# Для preview/production укажите полный адрес бэкенда, например:
# VITE_API_BASE=http://127.0.0.1:8000
VITE_API_BASE=
